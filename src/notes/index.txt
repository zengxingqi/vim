// 动态注册加载一个新的store模块
store.registerModule('c', {
  state: {
    name: '[name]'
  }
})

1、用户连接socket => socketid
  1.1、用户注册 => 用户登记组
  1.2、用户登录 => 用户在线池
  1.3、公共开放区
2、拉取数据
  2.1、
3、消息推送
  3.1、

// sender    发送者
// receiver  接收者
